# CI Environment Variables
DJANGO_SECRET_KEY=ci-secret-key-for-testing-purposes-only
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (matches ci.yml service)
DB_NAME=testdb
DB_USER=testuser
DB_PASSWORD=testpass
DB_HOST=localhost
DB_PORT=5432
