# Core framework
Django>=5.1,<5.2
djangorestframework>=3.15,<4.0

# PostgreSQL adapter
psycopg2-binary>=2.9,<3.0

# Environment variable management
django-environ>=0.11,<1.0

# CORS: allow frontend (different port) to call the API
django-cors-headers>=4.3,<5.0

# API documentation: auto-generates OpenAPI 3.0 schema + Swagger UI
drf-spectacular>=0.27,<1.0

# Async task queue (Celery + Redis broker)
celery>=5.4,<6.0
redis>=5.0,<6.0

# Code quality (matching CI pipeline)
black>=24.0
isort>=5.13
flake8>=7.0

# Testing (matching CI pipeline)
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
